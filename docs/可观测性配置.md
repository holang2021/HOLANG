# OpenTelemetry 可观测性配置

## 架构

```
OPS App → OTEL SDK (JS/Python) → OTEL Collector → Grafana / Jaeger
                                 ├── Metrics → Prometheus → Grafana
                                 ├── Traces  → Jaeger
                                 └── Logs    → Loki → Grafana
```

## SDK 接入（Node.js 示例）

```typescript
// otel.ts — 在应用入口处引入
import { NodeSDK } from '@opentelemetry/sdk-node';
import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-http';
import { OTLPMetricExporter } from '@opentelemetry/exporter-metrics-otlp-http';
import { HttpInstrumentation } from '@opentelemetry/instrumentation-http';
import { ExpressInstrumentation } from '@opentelemetry/instrumentation-express';

const sdk = new NodeSDK({
  traceExporter: new OTLPTraceExporter({
    url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT || 'http://localhost:4318/v1/traces',
  }),
  metricExporter: new OTLPMetricExporter({
    url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT || 'http://localhost:4318/v1/metrics',
  }),
  instrumentations: [
    new HttpInstrumentation(),
    new ExpressInstrumentation(),
  ],
});

sdk.start();
```

## RED 指标定义

| 指标 | 含义 | 告警阈值 |
|------|------|---------|
| **Rate** | 请求速率（req/s） | 突降 50% → Warning |
| **Error** | 错误率（5xx/total） | > 1% → Warning, > 5% → Critical |
| **Duration** | P95 延迟 | > 2× 基线 → Warning, > 5× → Critical |

## 关键 Span 定义

```
Bug 创建流程:
  POST /api/bugs → validate → DB insert → notify → DONE

Bug 修复流程:
  PATCH /api/bugs/:id/status → validate → DB update → notify QA → DONE

验收流程:
  POST /api/acceptance → aggregate → score → report → DONE
```

## 结构化日志格式

```json
{
  "timestamp": "2026-06-15T10:00:00Z",
  "level": "INFO",
  "service": "ops-bug-tracker",
  "trace_id": "abc123",
  "span_id": "def456",
  "message": "Bug status changed",
  "fields": {
    "bug_id": 42,
    "from_status": "open",
    "to_status": "in_progress",
    "user": "dev-zhang"
  }
}
```

## Grafana 看板配置

### Bug 全链路看板
- Panel 1: Bug 创建→修复→验收 各阶段耗时分布（热力图）
- Panel 2: 各阶段错误率趋势（折线图）
- Panel 3: P95 延迟 vs 基线（双线对比图）
- Panel 4: Top 10 慢 API（表格）

### 版本质量看板
- Panel 1: Bug 创建趋势（按版本/天）
- Panel 2: Bug 修复效率（MTTR 趋势）
- Panel 3: 逃逸率（生产 Bug / 总 Bug）
- Panel 4: 重开率趋势
